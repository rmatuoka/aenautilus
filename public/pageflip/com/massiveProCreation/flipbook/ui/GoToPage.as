package com.massiveProCreation.flipbook.ui{	// IMPORTS	import flash.display.Sprite;	import flash.display.MovieClip;	import flash.text.TextField;	import flash.events.MouseEvent;		import com.greensock.TweenMax;	import com.greensock.easing.*;		import com.massiveProCreation.flipbook.FlipBook;	public class GoToPage extends Sprite	{		public function GoToPage()		{			super();			// add event lsiteners to the go button			this.go.buttonMode = true;			this.go.mouseChildren = false;			this.go.addEventListener(MouseEvent.ROLL_OVER, goOver, false, 0, true);			this.go.addEventListener(MouseEvent.ROLL_OUT, goOut, false, 0, true);			this.go.addEventListener(MouseEvent.CLICK, goClick, false, 0, true);		}		private function goOver (e:MouseEvent):void {			// roll over event handler			TweenMax.to(e.target, 1, {alpha:0.8, ease:Expo.easeOut});		}		private function goOut (e:MouseEvent):void {			// roll out event handler			TweenMax.to(e.target, 1, {alpha:1, ease:Expo.easeOut});		}		private function goClick (e:MouseEvent):void {			// click event handler, call the gotoPage function of the FLipBook			if(int(this.page.text) != 0)				FlipBook(this.parent).gotoPage(int(this.page.text)-1, true);		}		}}